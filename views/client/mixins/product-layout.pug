include product-item.pug

mixin product-grid(products)
     .row
        each items in products
            +product-item(items)


//- mixin product-list(products)
//-      .row
//-         each items in products
//-             +product-item(items)